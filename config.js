// config.js
// Set this to your Railway backend base URL (no trailing slash), e.g.
// https://your-railway-app.up.railway.app
export const BASE_URL = "https://mascot.academictechnexus.com";

// Optional: Calendly link for "Book demo" CTA
export const CALENDLY_LINK = "https://calendly.com/yourlink";

// Slack webhook (if frontend will call your backend /api/lead)
export const SLACK_WEBHOOK = ""; // not used by frontend directly
